import pytest

# assert: Assert is a keyword which is used for comparison.


def test_validation_001():
    x = 10
    y = 20
    assert x == y


def test_validation_002():
    x = 10
    y = 20
    assert not x == y



# We will get by default only Failed test case reports.








